<UserControl x:Class="Taadol.Views.BranchArchiveListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:controls="clr-namespace:Taadol.Controls"
             d:DesignHeight="700"
             d:DesignWidth="1500"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
           
    FlowDirection="RightToLeft"
             Loaded="BranchArchiveListView_Loaded"
             FontFamily="{StaticResource IRANSans}"
             Background="#FFFFFF">

    <UserControl.Resources>
        <Style x:Key="EllipseLeft" TargetType="Ellipse">
            <Setter Property="Width" Value="25" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Fill" Value="#FFF8ED" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="-31,0,0,0" />
            <Setter Property="Panel.ZIndex" Value="-1" />
        </Style>
        <Style x:Key="EllipseRight" TargetType="Ellipse">
            <Setter Property="Width" Value="25" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Fill" Value="#FFF8ED" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,-31,0" />
            <Setter Property="Panel.ZIndex" Value="-1" />
        </Style>
        
    </UserControl.Resources>

    <Border x:Name="RootBorder"
            Background="#FFFFFF"
            CornerRadius="4"
            BorderBrush="#E5E7EB"
            BorderThickness="0"
            ClipToBounds="True">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- ═══ هدر ═══ -->
            <Border Grid.Row="0"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,0,0,1"
                    Padding="0,12"
                    Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Border Cursor="Hand"
                                Background="Transparent"
                                Margin="20,0,10,0"
                                MouseLeftButtonDown="HeaderClose_Click">
                            <svg:SvgViewbox FlowDirection="LeftToRight"
                                            Width="16"
                                            Height="16"
                                            Source="/Assets/Icons/closeTHIN.svg" />
                        </Border>
                        <TextBlock Text="آرشیو شعبه"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#0D2159"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══ محتویات اصلی ═══ -->
            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- ═══ نوار فیلتر + آپلود ═══ -->
                <Border Grid.Row="0"
                        Background="#ffffff"
                        CornerRadius="8"
                        Padding="0"
                        Margin="12,0,0,0"
                        BorderBrush="#ffffff"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- فیلترها -->
                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- ردیف اول: نام شرکت + نام شعبه -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" HorizontalAlignment="Left">
                                    <TextBlock Text="نام شرکت" Style="{StaticResource FormLabel}" />
                                    <ComboBox x:Name="CompanyComboBox"
                                              Style="{StaticResource SearchableComboBox}"
                                              ItemContainerStyle="{StaticResource SearchableComboBoxItem}"
                                              Width="300"
                                              DisplayMemberPath="Title"
                                              SelectedItem="{Binding SelectedCompany}"
                                              SelectionChanged="CompanyComboBox_SelectionChanged" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                                    <TextBlock Text="نام شعبه" Style="{StaticResource FormLabel}" />
                                    <ComboBox x:Name="BranchComboBox"
                                              Style="{StaticResource SearchableComboBox}"
                                              ItemContainerStyle="{StaticResource SearchableComboBoxItem}"
                                              Width="300"
                                              DisplayMemberPath="Title"
                                              SelectedItem="{Binding SelectedBranch}"
                                              SelectionChanged="BranchComboBox_SelectionChanged" />
                                </StackPanel>
                            </Grid>

                            <!-- ردیف دوم: عنوان فایل + توضیحات -->
                            <Grid Grid.Row="1" Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" HorizontalAlignment="Left">
                                    <TextBlock Text="عنوان فایل" Style="{StaticResource FormLabel}" />
                                    <controls:ModernPersianTextBox x:Name="SearchBox"
                                                                   Width="300"
                                                                   Placeholder="جستجو کنید" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                                    <TextBlock Text="توضیحات" Style="{StaticResource FormLabel}" />
                                    <controls:ModernPersianTextBox x:Name="DescriptionSearchBox"
                                                                   Width="550"
                                                                   Placeholder="جستجو کنید" />
                                </StackPanel>
                            </Grid>
                        </Grid>

                        <!-- آپلود فایل سمت راست -->
                        <controls:FilePickerControl Grid.Column="2"
                                                    Margin="0,0,12,0"
                                                    FlowDirection="LeftToRight"
                                                     x:Name="FilePicker"
                                                     VerticalAlignment="Top" />
                    </Grid>
                </Border>

                <!-- ═══ خط جداکننده ═══ -->
                <Grid Grid.Row="1" Margin="0,5,0,5">
                    <Border Height="3">
                        <Border.Background>
                            <VisualBrush TileMode="Tile"
                                         Viewport="0,0,10,1"
                                         ViewportUnits="Absolute"
                                         Viewbox="0,0,10,1"
                                         ViewboxUnits="Absolute">
                                <VisualBrush.Visual>
                                    <Grid Width="10" Height="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="5" />
                                            <ColumnDefinition Width="5" />
                                        </Grid.ColumnDefinitions>
                                        <Rectangle Grid.Column="0" Fill="#BED1FF" />
                                        <Rectangle Grid.Column="1" Fill="Transparent" />
                                    </Grid>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Border.Background>
                    </Border>
                    <Ellipse Width="17" Margin="-9,0,0,0" Height="25" Fill="#FFF8ED"
                             HorizontalAlignment="Left" VerticalAlignment="Center" Panel.ZIndex="2" />
                    <Ellipse Width="17" Height="25" Margin="0,0,-9,0" Fill="#FFF8ED"
                             HorizontalAlignment="Right" VerticalAlignment="Center" Panel.ZIndex="2" />
                </Grid>

                <!-- ═══ نوار ابزار: سرچ + دکمه‌ها ═══ -->
                <Border Grid.Row="2"
                        Background="#ffffff"
                        BorderBrush="#E5E7EB"
                        BorderThickness="0,0,0,1"
                        Padding="12,0,0,6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- سرچ -->
                        <Border Grid.Column="0"
                                Background="#FFFFFF"
                                BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="4"
                                Margin="0,0,4,0"
                                Height="40"
                                Padding="5,0"
                                VerticalAlignment="Center"
                                Width="280">
                            <Grid FlowDirection="RightToLeft">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"
                                      Height="30">
                                    <TextBlock Text="جستجو در عنوان ، شرکت ، شعبه"
                                               FontSize="12"
                                               Foreground="#94A3B8"
                                               FontFamily="{StaticResource IRANSans}"
                                               FlowDirection="RightToLeft"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               IsHitTestVisible="False"
                                               Panel.ZIndex="1">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=HeaderSearchBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBox x:Name="HeaderSearchBox"
                                             Height="30"
                                             FontSize="12"
                                             Foreground="#1F2937"
                                             CaretBrush="#3B82F6"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="0,2"
                                             VerticalAlignment="Center"
                                             VerticalContentAlignment="Center"
                                             FlowDirection="RightToLeft"
                                             FontFamily="{StaticResource IRANSans}"
                                             TextAlignment="Left">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderThickness="0"
                                                                    CornerRadius="4">
                                                                <ScrollViewer x:Name="PART_ContentHost"
                                                                              Focusable="False"
                                                                              HorizontalScrollBarVisibility="Hidden"
                                                                              VerticalScrollBarVisibility="Hidden" />
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>

                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Border Width="1"
                                            Height="14"
                                            Background="#BED1FF"
                                            VerticalAlignment="Center" />
                                    <svg:SvgViewbox Width="14"
                                                    Height="14"
                                                    FlowDirection="LeftToRight"
                                                    Source="/Assets/Icons/search.svg"
                                                    VerticalAlignment="Center"
                                                    Margin="8,0,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- دکمه‌ها -->
                        <StackPanel Grid.Column="2"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    FlowDirection="LeftToRight">
                            <controls:ActionButton Text="حذف"
                                                   TextColor="#DC2626"
                                                   BorderColor="#E9F0FF"
                                                   IconPath="/Assets/Icons/trash.svg"
                                                   Margin="10,0,0,0"
                                                   Click="BtnDelete_Click" />
                            <controls:ActionButton Text="بروزرسانی"
                                                   TextColor="#2563EB"
                                                   IconPath="/Assets/Icons/arrow-reload-horizontal-2.svg"
                                                   Margin="6,0,0,0"
                                                   Click="BtnRefresh_Click" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- ═══ DataGrid ═══ -->
                <controls:UnifiedListView x:Name="ArchivesGrid"
                                           Grid.Row="3"
                                           Padding="0,0,10,0"
                                           Margin="12,6,12,5"
                                          ItemsSource="{Binding FilteredArchives}"
                                          PagesSource="{Binding Pages}"
                                          PageInfoContent="{Binding PageInfoText}"
                                          IsLoading="{Binding IsLoading}">
                    <controls:UnifiedListView.Columns>

                            <!-- تاریخ بارگذاری -->
                            <DataGridTextColumn Header="تاریخ بارگذاری" Width="200" MinWidth="160"
                                                Binding="{Binding CreationDate}"
                                                ElementStyle="{StaticResource BaseTextColumnStyle}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource UnifiedGridColumnHeaderStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                                        <Setter Property="BorderBrush" Value="#E0E7FF" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- شرکت -->
                            <DataGridTextColumn Header="شرکت" Width="180" MinWidth="140"
                                                Binding="{Binding CompanyName}"
                                                ElementStyle="{StaticResource BaseTextColumnStyle}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource UnifiedGridColumnHeaderStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                                        <Setter Property="BorderBrush" Value="#E0E7FF" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- شعبه -->
                            <DataGridTextColumn Header="شعبه" Width="180" MinWidth="140"
                                                Binding="{Binding BranchTitle}"
                                                ElementStyle="{StaticResource FullNameColumnStyle}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource UnifiedGridColumnHeaderStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                                        <Setter Property="BorderBrush" Value="#E0E7FF" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- عنوان فایل -->
                            <DataGridTextColumn Header="عنوان فایل" Width="200" MinWidth="160"
                                                Binding="{Binding Title}"
                                                ElementStyle="{StaticResource FullNameColumnStyle}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource UnifiedGridColumnHeaderStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                                        <Setter Property="BorderBrush" Value="#E0E7FF" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- ایکون فایل -->
                            <DataGridTemplateColumn Header="فایل" Width="80" MinWidth="60">
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource UnifiedGridColumnHeaderStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                                        <Setter Property="BorderBrush" Value="#E0E7FF" />
                                    </Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <svg:SvgViewbox Width="20" Height="20"
                                                        Source="{Binding FileIconSource}"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- توضیحات -->
                            <DataGridTextColumn Header="توضیحات" Width="2.5*" MinWidth="200"
                                                Binding="{Binding Description}"
                                                ElementStyle="{StaticResource BaseTextColumnStyle}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource UnifiedGridColumnHeaderStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                                <DataGridTextColumn.CellStyle>
                                    <Style TargetType="DataGridCell" BasedOn="{StaticResource UnifiedGridCellStyle}">
                                        <Setter Property="BorderThickness" Value="0,0,1,0" />
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                        </controls:UnifiedListView.Columns>
                </controls:UnifiedListView>

                <!-- ═══ نوار خلاصه زیر گرید ═══ -->
                <Border Grid.Row="4"
                        Background="#ffffff"
                        BorderBrush="#E5E7EB"
                        x:Name="SummaryBorder"
                        BorderThickness="1"
                        Height="53"
                        CornerRadius="4"
                        Margin="0,2,0,2"
                        Padding="16,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- تعداد انتخاب شده -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock x:Name="SelectedSummaryText"
                                       Text="آرشیو انتخاب شده (0)"
                                       FontSize="14" FontWeight="SemiBold"
                                       Foreground="#1F2937"
                                       VerticalAlignment="Center" />
                            <Border Width="1" Height="14" Background="#E5E7EB" Margin="8,0" VerticalAlignment="Center" />
                        </StackPanel>

                        <!-- تعداد کل -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="تعداد کل"
                                       FontSize="14" FontWeight="SemiBold"
                                       Foreground="#1F2937"
                                       VerticalAlignment="Center" />
                            <TextBlock x:Name="TotalCountText"
                                       Text="۰ مورد"
                                       FontSize="14" FontWeight="Medium"
                                       Foreground="#2563EB"
                                       Margin="8,0"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>
                </Border>

            </Grid>

            <!-- ═══ فوتر (دکمه‌ها) ═══ -->
            <Border Grid.Row="2"
                    Margin="0,10,0,0"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12"
                    Background="White"
                    SnapsToDevicePixels="True"
                    UseLayoutRounding="True">
                <Grid Margin="16,10" FlowDirection="RightToLeft">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".25*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width=".15*" />
                    </Grid.ColumnDefinitions>

                    <!-- سمت راست: ذخیره -->
                    <controls:IconButton Grid.Column="0"
                                         x:Name="SaveButton"
                                         ButtonText="ذخیره"
                                         IconSource="/Assets/Icons/save.svg"
                                         ButtonCommand="{Binding SaveCommand}"
                                         ButtonBackground="#2667FF"
                                         CornerRadius="4" />

                    <!-- وسط: انصراف -->
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                Margin="0,0,20,0"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"
                                Cursor="Hand"
                                MouseLeftButtonUp="CancelButton_Click">
                        <svg:SvgViewbox Source="/Assets/Icons/redo.svg"
                                        Width="14" Height="14"
                                        FlowDirection="LeftToRight"
                                        Margin="6,0,15,0" />
                        <TextBlock Text="انصراف"
                                   FontFamily="{StaticResource IRANSans}"
                                   FontSize="14"
                                   Foreground="#2667FF"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- سمت چپ: ذخیره موقت -->
                    <controls:CustomOutlinedButton Grid.Column="2"
                                                   Text="ذخیره موقت"
                                                   HorizontalAlignment="Stretch"
                                                   BorderBrush="#2667FF"
                                                   Foreground="#2667FF">
                        <controls:CustomOutlinedButton.Icon>
                            <svg:SvgViewbox Source="/Assets/Icons/Black.svg"
                                            Width="16" Height="16" />
                        </controls:CustomOutlinedButton.Icon>
                    </controls:CustomOutlinedButton>
                </Grid>
            </Border>



            <!-- ═══ Decorative Ellipses ═══ -->
            <Ellipse Style="{StaticResource EllipseLeft}" Grid.Row="1" Grid.ColumnSpan="3" Margin="-18,0,0,0" />
            <Ellipse Style="{StaticResource EllipseRight}" Grid.Row="1" Grid.ColumnSpan="3" Margin="0,0,-18,0" />
        </Grid>
    </Border>
</UserControl>
