<UserControl x:Class="Taadol.Controls.IconButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svg="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf"
             x:Name="Root"
             Width="Auto">

    <Button Command="{Binding ButtonCommand, ElementName=Root}"
            Background="{Binding ButtonBackground, ElementName=Root}"
            Foreground="{Binding ButtonForeground, ElementName=Root}"
            Height="{Binding ButtonHeight, ElementName=Root}"
            MinWidth="{Binding ButtonWidth, ElementName=Root}"
            Width="Auto"
            BorderThickness="0">
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="{Binding CornerRadius, ElementName=Root}"
                        Padding="12,0"
                        SnapsToDevicePixels="True">
                    <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                </Border>
                <ControlTemplate.Triggers>
                    
                    <Trigger Property="IsMouseOver"
                             Value="True">
                        <Setter TargetName="border"
                                Property="Opacity"
                                Value="0.90" />
                        <Setter Property="Cursor"
                                Value="Hand" />
                    </Trigger>
                    
                    <Trigger Property="IsPressed"
                             Value="True">
                        <Setter TargetName="border"
                                Property="Opacity"
                                Value="0.75" />
                        <Setter TargetName="border"
                                Property="RenderTransform">
                            <Setter.Value>
                                <ScaleTransform ScaleX="0.97"
                                                ScaleY="0.97" />
                            </Setter.Value>
                        </Setter>
                        <Setter TargetName="border"
                                Property="RenderTransformOrigin"
                                Value="0.5,0.5" />
                    </Trigger>
                    
                    <Trigger Property="IsEnabled"
                             Value="False">
                        <Setter TargetName="border"
                                Property="Opacity"
                                Value="0.4" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Button.Template>

        <StackPanel Orientation="Horizontal">
            <svg:SvgViewbox Width="{Binding IconSize, ElementName=Root}"
                            Height="{Binding IconSize, ElementName=Root}"
                            Source="{Binding IconSource, ElementName=Root}"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0" />
            <TextBlock Text="{Binding ButtonText, ElementName=Root}"
                       FontSize="{Binding TextSize, ElementName=Root}"
                       FontFamily="{StaticResource IRANSans}"
                       VerticalAlignment="Center" />
        </StackPanel>
    </Button>
</UserControl>