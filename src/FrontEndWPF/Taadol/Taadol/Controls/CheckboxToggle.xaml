<UserControl x:Class="Taadol.Controls.CheckboxToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignWidth="200"
             d:DesignHeight="30"
             Width="Auto"
             Height="Auto"
             HorizontalAlignment="Left"
             VerticalAlignment="Center"
             x:Name="Root">

    <ToggleButton x:Name="InnerToggle"
                  Content="{Binding LabelContent, ElementName=Root}"
                  Cursor="Hand"
                  Width="Auto"
                  Height="Auto"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center"
                  Checked="InnerToggle_Checked"
                  Unchecked="InnerToggle_Unchecked">
        <ToggleButton.Template>
            <ControlTemplate TargetType="ToggleButton">
                
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center">

                    
                    <Border x:Name="CheckBoxBorder"
                            Width="20"
                            Height="20"
                            BorderThickness="1.5"
                            BorderBrush="#CBD5E1"
                            CornerRadius="4"
                            Background="#FFFFFF"
                            FlowDirection="LeftToRight"
                            VerticalAlignment="Center">
                        
                        <svg:SvgViewbox x:Name="CheckMark"
                                        Width="8"
                                        Height="8"
                                        Source="/Assets/Icons/wh_check.svg"
                                        FlowDirection="LeftToRight"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Visibility="Collapsed" />
                    </Border>

                    
                    <ContentPresenter Content="{TemplateBinding Content}"
                                      VerticalAlignment="Center">
                        <ContentPresenter.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize"
                                        Value="16" />
                                <Setter Property="FontWeight"
                                        Value="Medium" />
                                <Setter Property="FontFamily"
                                        Value="{StaticResource IRANSans}" />
                                <Setter Property="Foreground"
                                        Value="#2B3F6C" />
                            </Style>
                        </ContentPresenter.Resources>
                    </ContentPresenter>

                </StackPanel>

                <ControlTemplate.Triggers>
                    <Trigger Property="IsChecked"
                             Value="True">
                        <Setter TargetName="CheckBoxBorder"
                                Property="BorderBrush"
                                Value="#2667FF" />
                        <Setter TargetName="CheckBoxBorder"
                                Property="Background"
                                Value="#2667FF" />
                        <Setter TargetName="CheckBoxBorder"
                                Property="BorderThickness"
                                Value="0" />
                        <Setter TargetName="CheckMark"
                                Property="Visibility"
                                Value="Visible" />
                    </Trigger>
                    <Trigger Property="IsChecked"
                             Value="False">
                        <Setter TargetName="CheckBoxBorder"
                                Property="BorderBrush"
                                Value="#CBD5E1" />
                        <Setter TargetName="CheckBoxBorder"
                                Property="Background"
                                Value="#FFFFFF" />
                        <Setter TargetName="CheckBoxBorder"
                                Property="BorderThickness"
                                Value="1.5" />
                        <Setter TargetName="CheckMark"
                                Property="Visibility"
                                Value="Collapsed" />
                    </Trigger>
                    <Trigger Property="IsMouseOver"
                             Value="True">
                        <Setter TargetName="CheckBoxBorder"
                                Property="BorderBrush"
                                Value="#2667FF" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </ToggleButton.Template>
    </ToggleButton>
</UserControl>
