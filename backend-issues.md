# گزارش مشکلات بک‌اند (backend-issues.md)

> این گزارش حاصل کار فرانت‌اند (فاز ۲ — لیست بانک‌ها و صندوق‌ها) است. مطابق قانون پروژه، هیچ تغییری در کد بک‌اند اعمال نشده؛ این موارد برای بررسی و رفع توسط تیم بک‌اند مستند شده‌اند.

---

## ۱. رکوردهای حذف‌شده (soft delete) در لیست‌ها نمایش داده می‌شوند

- **مسیر/فایل‌ها:**
  - `BankManagement.Infrastructure.EFCore/Repository/BankRepository.cs` → متدهای `GetBanks()` و `Search()`
  - `FinancialManagement.Infrastructure.EFCore/Repository/FundRepository.cs` → متدهای `GetFunds()` و `Search()`
- **رفتار فعلی:** کوئری‌ها `IsDeleted` را فیلتر نمی‌کنند؛ بعد از فراخوانی `Remove(id)` (حذف نرم که `IsDeleted=true` می‌کند)، رکورد حذف‌شده همچنان در پاسخ لیست برمی‌گردد.
- **رفتار موردانتظار:** رکوردهای حذف‌شده نباید در لیست عادی دیده شوند (فیلتر `.Where(x => !x.IsDeleted)`) — هم‌راستا با ریپازیتوری‌های خواهر مثل `CompanyBankAccountRepository` و `ChequeBookRepository` که این فیلتر را دارند.
- **مراحل بازتولید:**
  1. یک بانک/صندوق ثبت کنید (فرم «بانک جدید» / «صندوق جدید»).
  2. در «لیست بانک‌ها» / «لیست صندوق‌ها» آن را حذف کنید (دکمه حذف).
  3. لیست را رفرش کنید — رکورد حذف‌شده همچنان نمایش داده می‌شود.
- **نکته تکمیلی:** `GetDetails()` بانک و صندوق هم فیلتر `IsDeleted` ندارد؛ بهتر است رکورد حذف‌شده قابل ویرایش نباشد.

---

## ۲. عدم وجود `IsActive` / `IsDeleted` در ViewModel های لیست

- **مسیر/فایل‌ها:**
  - `BankManagement.Application.Contracts/Bank/BankViewModel.cs`
  - `FinancialManagement.Application.Contracts/Fund/FundViewModel.cs`
  - (نمونه درست: `CompanyBankAccountRepository` مقادیر `IsActive` و `IsDeleted` را در ViewModel نگاشت می‌کند)
- **رفتار فعلی:** موجودیت‌ها (`Banks`، `Funds`) پرچم `IsActive` دارند و سرویس‌ها متدهای `Activate`/`Deactivate`/`Restore` دارند، اما این فیلدها در ViewModel لیست نگاشت نشده‌اند.
- **رفتار موردانتظار:** افزودن `bool IsActive` و `bool IsDeleted` به `BankViewModel` و `FundViewModel` و نگاشت آن‌ها در کوئری‌ها.
- **اثر روی فرانت:** بدون این فیلدها نمی‌توان تب‌های «فعال / غیرفعال» (که در همه لیست‌های دیگر مثل شرکت‌ها و دوره‌های مالی هست) را نشان داد و امکان صفحه آرشیو/بازیابی وجود ندارد.

---

## ۳. ناسازگاری قالب تاریخ `CreationDate`

- **مسیر/فایل‌ها:**
  - `BankRepository` → `x.CreationDate.ToString()` (وابسته به culture و شامل ساعت)
  - `FundRepository` → `x.CreationDate.ToString("yyyy/MM/dd")`
- **رفتار موردانتظار:** خروجی یکسان و پایدار (مثلاً همیشه `"yyyy/MM/dd"` یا یک `DateTime` تایپ‌شده) تا فرانت نیازی به مدیریت دو قالب متفاوت نداشته باشد.

---

## ۴. نبود ترتیب مشخص (Ordering) و صفحه‌بندی سمت سرور

- **مسیر/فایل‌ها:** `BankRepository.Search()` (بدون `OrderBy`)
- **رفتار فعلی:** ترتیب ردیف‌ها تضمین‌شده نیست؛ صفحه‌بندی هم اکنون در حافظه فرانت انجام می‌شود (سرویس `List<T>` کامل برمی‌گرداند).
- **رفتار موردانتظار (آینده):** افزودن `OrderBy` پایدار در کوئری‌ها و در نظر گرفتن صفحه‌بندی سمت سرور وقتی حجم داده بزرگ شود.
- **اولویت:** پایین — فعلاً با حجم کم داده (۲۷ بانک / ۵۰۰ صندوق) مشکلی ایجاد نمی‌کند.

---

## ۵. رکوردهای جدید بانک و صندوق به‌صورت پیش‌فرض غیرفعال ایجاد می‌شوند

- **مسیر/فایل‌ها:**
  - `BankManagement.Domain/Bank/BankAgg/Banks.cs` → سازنده `Banks`
  - `FinancialManagement.Domain/FundAgg/Funds.cs` → سازنده `Funds`
  - `01_FrameWork/Domain/EntityBase.cs` → مقدار پیش‌فرض `IsActive`
- **رفتار فعلی:** `EntityBase.IsActive` مقدار پیش‌فرض `false` دارد و سازنده‌های بانک و صندوق آن را به `true` تغییر نمی‌دهند؛ بنابراین رکورد جدید غیرفعال ذخیره می‌شود. چک‌باکس «بانک فعال است» در فرم نیز به فیلد مدل متصل نیست.
- **رفتار موردانتظار:** وضعیت فعال رکورد جدید باید از قرارداد ایجاد دریافت و اعتبارسنجی شود؛ در حالت پیش‌فرض فرم ایجاد، رکورد باید فعال ذخیره شود یا وضعیت انتخاب‌شده کاربر دقیقاً در دامنه اعمال شود.
- **اثر روی فرانت:** بعد از اضافه‌شدن تب‌های فعال/غیرفعال، بانک یا صندوق تازه‌ثبت‌شده ممکن است در فهرست فعال‌ها دیده نشود.

---

## ۶. نبود قرارداد/سرویس دریافت حساب‌های قابل انتخاب برای ثبت صندوق

- **مسیر/فایل مرتبط:** `src/FrontEndWPF/Taadol/Taadol/ViewModels/NewFundViewModel.cs`
- **رفتار فعلی:** ایجاد صندوق به `AccountId` نیاز دارد، اما سرویس بارگذاری فرم فقط شعبه‌ها را دریافت می‌کند و `Accounts` هیچ‌وقت پر نمی‌شود. در بخش‌های بک‌اند نیز قرارداد کاربردی مشخصی برای دریافت حساب‌های فعال و مناسب صندوق در دسترس این فرم وجود ندارد.
- **رفتار موردانتظار:** یک قرارداد/سرویس مشخص برای دریافت حساب‌های فعال، حذف‌نشده و مجاز برای صندوق ارائه شود؛ پاسخ باید حداقل شامل `Id` و `Title` باشد.
- **اثر روی فرانت:** کاربر نمی‌تواند مقدار معتبر `AccountId` انتخاب کند و ثبت صندوق عملاً با پیام «لطفاً حساب صندوق را انتخاب کنید» متوقف می‌شود.
- **وضعیت:** نیازمند هماهنگی قرارداد بین بک‌اند و فرانت است؛ در این مورد هیچ تغییری در بک‌اند اعمال نشده است.

---

## ۷. اعتبارسنجی نشدن شناسه‌های مرجع هنگام ایجاد بانک و صندوق

- **مسیر/فایل‌ها:**
  - `BankManagement.Application/BankApplication.cs` → متد `Create`
  - `FinancialManagement.Application/FundApplication.cs` → متد `Create`
- **رفتار فعلی:** قبل از ایجاد بانک، وجود و فعال‌بودن `BankTypeId` بررسی نمی‌شود. هنگام ایجاد صندوق نیز وجود/فعال‌بودن `BranchId` و وجود/مجازبودن `AccountId` بررسی نمی‌شود.
- **رفتار موردانتظار:** سرویس ایجاد باید شناسه‌های مرجع را اعتبارسنجی کرده و در صورت نامعتبر بودن، `OperationResult.Failed(...)` با پیام قابل نمایش برگرداند؛ خطای پایگاه‌داده یا ثبت رکورد ناقص نباید به فرانت منتقل شود.

---

## ۸. ناسازگاری جستجوی رکوردهای حذف‌شده با منطق تکراری‌بودن عنوان صندوق

- **مسیر/فایل‌ها:**
  - `FinancialManagement.Infrastructure.EFCore/Repository/FundRepository.cs` → متد `Search`
  - `FinancialManagement.Application/FundApplication.cs` → بررسی `duplicate`
- **رفتار فعلی:** `FundRepository.Search()` رکوردهای `IsDeleted=true` را نیز برمی‌گرداند و منطق جلوگیری از عنوان تکراری از همین جستجو استفاده می‌کند؛ در نتیجه صندوق حذف‌شده همچنان مانع ثبت صندوق جدید با همان عنوان می‌شود. این رفتار با بانک ناسازگار است، چون `BankApplication` از `Exists` استفاده می‌کند که رکورد حذف‌شده را فیلتر می‌کند.
- **رفتار موردانتظار:** سیاست عنوان تکراری برای بانک و صندوق یکسان شود؛ معمولاً رکورد حذف‌شده نباید در فهرست عادی و بررسی تکراری‌بودن فعال لحاظ شود، مگر اینکه سیاست کسب‌وکار خلاف آن باشد.

---

## ۹. پشتیبانی نشدن `UniqueCode` در قرارداد ایجاد بانک

- **مسیر/Contract مرتبط:**
  - `BankManagement.Application.Contracts/Bank/CreateBank.cs`
  - `BankManagement.Application/BankApplication.cs` → متد `Create`
  - `BankManagement.Domain/Bank/BankAgg/Banks.cs`
- **رفتار فعلی:** فرم فرانت مقدار `UniqueCode` را دریافت و در ViewModel نگه‌داری می‌کند، اما `CreateBank` فقط فیلدهای `Title`، `Country`، `Description`، `Logo` و `BankTypeId` را دارد. این مقدار در زمان فراخوانی `IBankApplication.Create` ارسال نمی‌شود و در موجودیت `Banks` و عملیات ایجاد نیز فیلدی برای آن وجود ندارد.
- **رفتار موردانتظار:** Contract ایجاد بانک باید مشخص کند که شناسه یکتا چگونه و با چه نوعی دریافت می‌شود، اعتبارسنجی می‌شود و در موجودیت/پایگاه‌داده ذخیره می‌گردد؛ حالت اتوماتیک و دستی نیز باید رفتار مشخص داشته باشد.
- **اثر روی فرانت:** مقدار واردشده در فیلد شناسه یکتا در حال حاضر فقط نمایشی است و بعد از ثبت بانک در داده ذخیره‌شده قابل بازیابی نیست.
- **وضعیت فاز ۲:** تا زمان ارائه Contract، فیلد در UI و ViewModel فعال است اما عمداً وارد درخواست ثبت نمی‌شود و با TODO در کد فرانت مشخص شده است.

---

## ۱۰. پشتیبانی نشدن `IsActive` در قرارداد ایجاد بانک

- **مسیر/Contract مرتبط:**
  - `BankManagement.Application.Contracts/Bank/CreateBank.cs`
  - `BankManagement.Application/BankApplication.cs` → متد `Create`
  - `BankManagement.Domain/Bank/BankAgg/Banks.cs`
- **رفتار فعلی:** فرم فرانت وضعیت فعال/غیرفعال را در `NewBankViewModel.IsActive` نگه‌داری و به کنترل UI متصل می‌کند، اما `CreateBank` فیلد `IsActive` ندارد و `BankApplication.Create` نیز این وضعیت را به موجودیت منتقل نمی‌کند. همچنین سازنده `Banks` وضعیت فعال را صریحاً تعیین نمی‌کند و مقدار پیش‌فرض `EntityBase.IsActive` استفاده می‌شود.
- **رفتار موردانتظار:** Contract ایجاد باید وضعیت `IsActive` را دریافت کند یا سیاست مقدار پیش‌فرض آن را به‌صورت رسمی تعیین کند؛ مقدار انتخاب‌شده کاربر باید پس از ثبت قابل مشاهده و بازیابی باشد.
- **اثر روی فرانت:** تیک فعال/غیرفعال فعلاً روی رکورد ثبت‌شده اثر قطعی ندارد و ممکن است وضعیت ذخیره‌شده با انتخاب کاربر یکسان نباشد.
- **وضعیت فاز ۲:** کنترل UI و binding فرانت تکمیل شده است، اما ارسال و ذخیره مقدار تا زمان اصلاح Contract عمداً انجام نمی‌شود.

---

## ۱۱. نامشخص بودن/ناقص بودن رفتار ذخیره‌سازی لوگوی بانک

- **مسیر/Contract مرتبط:**
  - `BankManagement.Application.Contracts/Bank/CreateBank.cs` → فیلد `Logo`
  - `BankManagement.Domain/Bank/BankAgg/Banks.cs` → فیلد `Logo`
  - `BankManagement.Infrastructure.EFCore/Mapping/BankMapping.cs`
  - فرانت: `NewBankViewModel` و `ImagePickerControl`
- **رفتار فعلی مشاهده‌شده از مسیر کد:** کنترل انتخاب تصویر یک مسیر فایل محلی مطلق را در `Logo` قرار می‌دهد و همان مقدار به `CreateBank.Logo` ارسال می‌شود. بک‌اند این مقدار را بدون قرارداد مشخص برای آپلود، کپی فایل، تبدیل به شناسه/URL یا اعتبارسنجی مسیر، مستقیماً در فیلد رشته‌ای موجودیت قرار می‌دهد؛ Mapping نیز فقط ستون رشته‌ای را نگاشت می‌کند. در لیست بانک‌ها مقدار `Logo` از ViewModel خوانده می‌شود اما در UI لیست فعلی مصرف نمی‌شود.
- **رفتار موردانتظار:** Contract باید نوع و چرخه عمر لوگو را مشخص کند: آپلود فایل یا دریافت URL/شناسه، محل ذخیره، اعتبارسنجی نوع و حجم، نام‌گذاری، دسترسی مجدد و رفتار حذف/ویرایش تصویر. پاسخ ثبت/جزئیات نیز باید مقدار قابل استفاده برای نمایش در فرانت برگرداند.
- **اثر روی فرانت:** فرانت فقط مسیر محلی را ارسال می‌کند؛ این مسیر روی سیستم کاربر دیگر الزاماً معتبر نیست و تضمینی برای نمایش لوگو بعد از ثبت یا روی سیستم دیگر وجود ندارد.
- **وضعیت فاز ۲:** انتخاب لوگو و binding مسیر در فرانت تکمیل شده است؛ صحت ذخیره و بازیابی واقعی لوگو تا تعیین قرارداد آپلود/URL توسط بک‌اند قابل تأیید نیست.

---

## ۱۲. اتمام Connection Pool هنگام بارگذاری دوره‌های مالی

- **مسیر/نقطه مشاهده خطا:**
  - `GeneralInfoManagement.Infrastructure.EFCore/Repository/FinancialPeriodRepository.cs` → متد `GetFinancialPeriods()`، هنگام اجرای `ToList()` در خط ۱۹
  - فراخوانی از `GeneralInfoManagement.Application/FinancialPeriodAppliction.cs` → متد `GetFinancialPeriods()`
  - فراخوانی فرانت از `src/FrontEndWPF/Taadol/Taadol/Controls/Inputs/YearSelectorControl.xaml.cs` → متد `LoadPeriodsAsync()`
- **خطای مشاهده‌شده:** `Microsoft.Data.SqlClient` قبل از دریافت اتصال از Pool، تا پایان زمان انتظار منتظر مانده است؛ پیام خطا نشان می‌دهد تمام اتصال‌های Pool در حال استفاده بوده‌اند یا اتصال‌ها به‌موقع آزاد نشده‌اند.
- **رفتار فعلی:** اجرای کوئری در `FinancialPeriodRepository` با خطای خود کوئری متوقف نشده، بلکه پیش از باز شدن اتصال جدید با خطای اتمام Pool مواجه شده است. از روی این Stack Trace به‌تنهایی نمی‌توان مشخص کرد اتصال‌ها به علت کندی/قفل بودن SQL Server، اجرای هم‌زمان درخواست‌ها، یا نگه‌داری نادرست Contextها آزاد نشده‌اند.
- **نکته فنی مرتبط:** در `GeneralInfoManagementBoostrapper`، Contextها با `AddDbContext` و عمر Scoped ثبت شده‌اند؛ اما در چند View، سرویس‌های Application دارای وابستگی به Context مستقیماً از Root `App.ServiceProvider` دریافت و در فیلد نگه‌داری می‌شوند. این الگو می‌تواند عمر Context را از Scope مورد انتظار طولانی‌تر کند و باید بررسی و اصلاح شود. نمونه‌ها در `NewFinancialPeriodView` و `FinancialPeriodListView` وجود دارند.
- **نکته فنی تکمیلی:** عملیات دیتابیس در `YearSelectorControl` به‌صورت synchronous داخل `Task.Run` اجرا می‌شود. لغو `Task` لزوماً کوئری SQL در حال اجرا را متوقف نمی‌کند؛ در صورت Refreshهای پشت‌سرهم ممکن است چند کوئری هم‌زمان در حال انتظار یا اجرا باقی بمانند.
- **رفتار موردانتظار:** هر عملیات دیتابیس باید در یک Scope مستقل اجرا شود، Context پس از پایان عملیات Dispose شود، کوئری‌های هم‌زمان و Refreshهای قبلی کنترل شوند، و علت اصلی کندی/قفل SQL Server با بررسی سمت سرور مشخص شود. در صورت نیاز، Timeout و Max Pool Size باید آگاهانه و پس از رفع علت اصلی تنظیم شوند؛ افزایش صرف Max Pool Size درمان ریشه‌ای نیست.
- **اثر روی فرانت:** انتخاب‌گر سال مالی و هر صفحه‌ای که دوره‌های مالی را بارگذاری می‌کند ممکن است با تأخیر، پیام خطای بارگذاری، یا عدم نمایش اطلاعات مواجه شود. این خطا در زمان `ToList()` دیده می‌شود، اما الزاماً به معنی خراب بودن Projection دوره مالی نیست.
- **مراحل بازتولید پیشنهادی:**
  1. برنامه را اجرا کنید و چند بار پشت‌سرهم صفحه یا انتخاب‌گر دوره مالی را بازخوانی کنید.
  2. هم‌زمان چند فرم/لیست وابسته به `GeneralInfoFakeDataContext` را باز یا Refresh کنید.
  3. وضعیت Connectionهای فعال SQL Server و درخواست‌های در حال اجرا را بررسی کنید.
  4. مشخص کنید آیا اتصال‌ها در سمت SQL Server با `blocking_session_id` مسدود هستند یا در برنامه Context/Scope بدون Dispose باقی مانده است.
- **وضعیت:** نیازمند بررسی مشترک بک‌اند، زیرساخت SQL Server و الگوی Scope در فرانت است؛ هیچ تغییری در بک‌اند برای این مورد اعمال نشده است.

---

## جمع‌بندی اولویت‌ها
1. بند ۱ — باگ واقعی کاربر (حذف‌شده‌ها در لیست می‌مانند)
2. بند ۵ — رکوردهای جدید بانک و صندوق غیرفعال ایجاد می‌شوند
3. بند ۲ — پیش‌نیاز UI استاندارد لیست‌ها (تب‌های فعال/غیرفعال و آرشیو)
4. بند ۶ و ۷ — پیش‌نیاز ثبت کامل و ایمن صندوق/بانک
5. بند ۸ — ناسازگاری سیاست حذف نرم و عنوان تکراری
6. بند ۳ و ۴ — به‌سازی و نظم کد
